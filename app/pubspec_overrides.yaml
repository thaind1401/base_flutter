# melos_managed_dependency_overrides: data,domain,initializer,resources,shared,js,win32,device_info_plus
dependency_overrides:
  # Override analyzer version to make custom_lint_builder compatible
  data:
    path: ../data
  device_info_plus: ^10.1.0
  domain:
    path: ../domain
  initializer:
    path: ../initializer
  js: ^0.7.0
  resources:
    path: ../resources
  shared:
    path: ../shared
  win32: ^5.8.0
